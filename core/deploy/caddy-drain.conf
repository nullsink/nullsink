[Service]
# Caddy stops accepting new connections immediately, but keeps existing streams alive while its upstream drains.
# Outlive nullsink-proxy's 60-second systemd stop window so the edge is never killed before billing settles.
TimeoutStopSec=70s
